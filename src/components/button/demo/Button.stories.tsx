import React from 'react';
import Button from '../index';
import '../style';

export default {
  title: 'Upgraded/Button',
  component: Button,
};

export const Demo = () => {
  return <Button size="large">
    123
  </Button>
}